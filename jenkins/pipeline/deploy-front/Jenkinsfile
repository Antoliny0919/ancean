pipeline {
  agent any

  environment {
    IMAGE_TAG = 'v5.0.0'
  }

  stages {
    stage('Build') {
      steps {
	      sh '''
          sh 
	      '''
      }
    }
    stage('Deploy Stag') {
      steps {
        sh '''
        echo "deploy stag env stage
        '''
      }
    }
    stage('Build Prod') {
      steps {
        sh '''
        echo Build Prod env stage"
        '''
      }
    }
  }
}