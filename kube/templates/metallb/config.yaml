apiVersion: v1
kind: ConfigMap
metadata:
  namespace: metallb-system
  name: {{ .Release.Name }}-matallb-config
  labels:
    ancean: {{ .Values.env }}
data:
  config: |
    address-pools:
    - name: default
      protocol: layer2
      addresses:
      - 192.168.1.100-192.168.1.200